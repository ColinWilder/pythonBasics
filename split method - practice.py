# the split method

string1 = "hello world fog dog dog fog"

for s in string1:
    print(string1.split(s))

#splitterText="d" # this would be an example of using a splitter within the split method other than the default (which is whitespace)
